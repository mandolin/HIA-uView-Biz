# Modules

This directory will contain optional business capability modules. Each module must have its own package metadata, public contract, configuration boundary, dependency audit and validation evidence before it joins the default distribution path.
