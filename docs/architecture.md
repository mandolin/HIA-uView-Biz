# Architecture overview / 架构概览

HIA-uView-Biz is a configurable, capability-composed UniApp business framework. Its first target is mini-program development, while its public contracts keep backend, identity, industry model, and presentation channel replaceable.

HIA-uView-Biz 是可配置、按能力组合的 UniApp 业务框架。其首要目标是小程序开发，同时其公开契约保持后端、身份、行业模型和呈现渠道可替换。

| Area / 区域 | Responsibility / 主责 | Boundary / 边界 |
| --- | --- | --- |
| `packages/core` | Composition rules, manifest validation, declared ports, and validation hooks / 组合规则、manifest 校验、已声明 port 与验证挂钩 | Does not own domain records, routes, transport, or credentials / 不拥有领域记录、路由、传输或凭据 |
| `packages/app-shell` | In-memory screen-state projection, action gate, mock capability gate, canonical failure presentation input, and retry command retention / 内存 screen-state 投影、action gate、mock capability gate、规范化 failure 呈现输入和 retry command 保留 | No Vue, UniApp, URL/router, platform navigation, storage, real identity, transport, or UI registration / 没有 Vue、UniApp、URL/router、平台导航、storage、真实身份、传输或 UI 注册 |
| `packages/adapter-runtime` | Explicit read-adapter declaration, injected exchange lifecycle, canonical-outcome boundary, redaction, and optional bounded memory cache / 显式 read-adapter 声明、注入式 exchange lifecycle、canonical-outcome 边界、脱敏与可选受限内存缓存 | No network client, backend SDK, environment, credential, storage, persistent cache, write operation, UI, or route / 没有网络 client、backend SDK、环境、credential、storage、持久缓存、写操作、UI 或 route |
| `packages/capability-runtime` | Explicit multi-unit install/enable/disable/uninstall state, dependency/conflict checks, unique ownership, invocation routing, and redacted snapshots / 显式多单元安装/启用/停用/卸载状态、依赖/冲突检查、唯一主责、调用路由与脱敏 snapshot | Process-local only; no package discovery/installation, hooks, automatic dependency activation, persistence, remote code, UI, or backend / 仅进程内；没有包发现/安装、hook、自动依赖启用、持久化、远程代码、UI 或后端 |
| `packages/adoption-runtime` | Complete adoption-profile validation, exact explicit-unit matching, dependency-first candidate activation, bounded presentation, receipt, and atomic in-memory replacement / 完整 adoption-profile 校验、精确显式单元匹配、依赖优先候选启用、受限呈现、receipt 与原子进程内替换 | No discovery, package-manager action, dynamic import, hook, persistence, migration, component loading, or external state / 没有发现、包管理器动作、动态 import、hook、持久化、迁移、组件加载或外部状态 |
| `modules` | Capability-oriented business ownership and schemas / 面向能力的业务主责与 schema | Separates business semantics from package delivery / 与包交付事实分离 |
| `extensions` | Explicit adapters and other documented extension packages; currently includes one neutral injected-wire fixture / 显式 adapter 与其他有文档的扩展包；当前包含一个中性注入式 wire fixture | No hidden coupling to application internals, real backend discovery, or implicit fallback / 不得隐式耦合应用内部实现、发现真实后端或隐式 fallback |
| `apps/example-catalog-query-detail-mp-weixin` | Versioned declarative profile, explicit mock/wire source, app-owned lifecycle-to-shell bridge, registered presentation projection, and controlled UniApp Vue 3 compiler fixture / 带版本声明式 profile、显式 mock/wire source、app-owned lifecycle-to-shell bridge、已登记呈现投影与受控 UniApp Vue 3 compiler fixture | Representative `mp-weixin` only; no dev server, source fallback, router, real backend adapter, real identity, write flow, arbitrary component/script, or release claim / 仅代表性 `mp-weixin`；没有 dev server、source 回退、router、真实后端 adapter、真实身份、写流程、任意组件/脚本或发布声明 |
| HIA-uView | Presentational UI input to the controlled fixture / 受控 fixture 的呈现 UI 输入 | Uses a reviewed local source guard, one-use local link, explicit SFC imports, and explicit style; no external `file:` package dependency / 使用已复审的本地 source guard、一次性本地 link、显式 SFC 导入和显式样式；没有外部 `file:` package dependency |

The detailed decision is [ADR-0001: Business composition and contract boundaries](adr/ADR-0001-biz-composition-and-contract-boundaries.md). It defines the module/implementation manifest split, canonical adapter outcomes, profile and channel projection, and declarative dynamic-presentation boundary.

详细决定见 [ADR-0001：业务组合与契约边界](adr/ADR-0001-biz-composition-and-contract-boundaries.md)。该 ADR 定义模块/实现包 manifest 分离、adapter 规范化结果、profile 与渠道投影，以及声明式动态呈现边界。

The current runtime implements the core's explicit manifest/profile/port composition boundary, deterministic neutral mock, pure application shell, a backend-agnostic adapter lifecycle, an explicit process-local capability lifecycle, and candidate-first complete-set adoption. The adoption runtime accepts only caller-supplied units, creates an isolated candidate, enforces exact selection and dependency order, validates host-registered presentation metadata, and switches runtime/presentation together after success. A changed implementation ID for the same module becomes a redacted `replace` receipt; no package, hook, migration, or external state is involved. The first composed set enables neutral `example.reference-data` before `example.catalog-query-detail`. The representative app uses that same dependency order for either its wire fixture or mandatory mock, bridges only enabled catalog invocation into app shell, and lets registered block IDs reveal only compiled template branches. Node acceptance exercises query, detail, back, empty/failure, detached snapshots, bounded adapter observation, adoption, disablement, conflict, replacement, and rollback. The controlled compiler then compiles the explicit-SFC HIA-uView projection for `mp-weixin` after verifying an operator-provided local UI source. The stack still does not implement package discovery/installation, lifecycle/migration scripts, persistent adoption state, a real HTTP/Directus adapter, real identity integration, industry package, write flow, URL/router, dynamic executable configuration, or published UI dependency.

当前运行时实现了 core 的显式 manifest/profile/port 组合边界、确定性中性 mock、纯应用 shell、backend-agnostic adapter lifecycle、显式进程内 capability lifecycle，以及候选优先完整集合采用。adoption runtime 只接受调用方提供的单元，创建隔离候选，强制精确选择与依赖顺序，校验宿主已登记呈现 metadata，并在成功后一起切换 runtime/presentation。同一 module 改变 implementation ID 会形成脱敏 `replace` receipt；不涉及 package、hook、migration 或外部状态。首个组合集合先启用中性 `example.reference-data`，再启用 `example.catalog-query-detail`。代表性 app 对 wire fixture 或必备 mock 使用同一依赖顺序，只把已启用 catalog 调用桥接给 app shell，并使已登记区块 ID 只能显示已编译 template 分支。Node 验收执行 query、detail、返回、empty/failure、隔离 snapshot、受限 adapter observation、adoption、停用、冲突、替换与回退。随后，受控 compiler 在校验操作者提供的本地 UI source 后，为 `mp-weixin` 编译显式 SFC 导入的 HIA-uView 投影。该 stack 仍不实现包发现/安装、lifecycle/migration script、持久采用状态、真实 HTTP/Directus adapter、真实身份集成、行业包、写流程、URL/router、动态可执行配置或已发布 UI dependency。

See the [representative slice contract / 代表性纵切契约](contracts/representative-mp-weixin-slice.md), [capability adoption contract / 能力采用契约](contracts/capability-adoption.md), [core API / core API](api/core.md), [application-shell API / 应用 shell API](api/app-shell.md), [adapter-runtime API / Adapter runtime API](api/adapter-runtime.md), [capability-runtime API / 能力 runtime API](api/capability-runtime.md), [adoption-runtime API / 采用 runtime API](api/adoption-runtime.md), and the ADR index for the implemented surface and its deliberate limits.

实现的 API 及其刻意限制见[代表性纵切契约](contracts/representative-mp-weixin-slice.md)、[能力采用契约](contracts/capability-adoption.md)、[core API](api/core.md)、[应用 shell API](api/app-shell.md)、[adapter-runtime API](api/adapter-runtime.md)、[能力 runtime API](api/capability-runtime.md)、[采用 runtime API](api/adoption-runtime.md)与 [ADR 索引](adr/README.md)。
