# Architecture overview / 架构概览

HIA-uView-Biz is a configurable, capability-composed UniApp business framework. Its first target is mini-program development, while its public contracts keep backend, identity, industry model, and presentation channel replaceable.

HIA-uView-Biz 是可配置、按能力组合的 UniApp 业务框架。其首要目标是小程序开发，同时其公开契约保持后端、身份、行业模型和呈现渠道可替换。

| Area / 区域 | Responsibility / 主责 | Boundary / 边界 |
| --- | --- | --- |
| `packages/core` | Composition rules, manifest validation, declared ports, and validation hooks / 组合规则、manifest 校验、已声明 port 与验证挂钩 | Does not own domain records, routes, transport, or credentials / 不拥有领域记录、路由、传输或凭据 |
| `packages/app-shell` | In-memory screen-state projection, action gate, mock capability gate, canonical failure presentation input, and retry command retention / 内存 screen-state 投影、action gate、mock capability gate、规范化 failure 呈现输入和 retry command 保留 | No Vue, UniApp, URL/router, platform navigation, storage, real identity, transport, or UI registration / 没有 Vue、UniApp、URL/router、平台导航、storage、真实身份、传输或 UI 注册 |
| `packages/adapter-runtime` | Explicit read-adapter declaration, injected exchange lifecycle, canonical-outcome boundary, redaction, and optional bounded memory cache / 显式 read-adapter 声明、注入式 exchange lifecycle、canonical-outcome 边界、脱敏与可选受限内存缓存 | No network client, backend SDK, environment, credential, storage, persistent cache, write operation, UI, or route / 没有网络 client、backend SDK、环境、credential、storage、持久缓存、写操作、UI 或 route |
| `packages/transport-operation-runtime` | Exact static local read-operation descriptor, complete reviewed handler-map validation, isolated adapter-private dispatch, and count-only observation / 精确静态本地 read-operation descriptor、完整已审阅 handler-map 校验、隔离 adapter-private dispatch 与仅计数 observation | No URL/HTTP vocabulary, backend SDK, credential, identity, async/retry/timeout/cancellation, command transport, persistence, dynamic loading, UI, or route / 没有 URL/HTTP 词汇、backend SDK、credential、identity、async/retry/timeout/cancellation、command transport、持久化、动态加载、UI 或 route |
| `packages/capability-runtime` | Explicit multi-unit install/enable/disable/uninstall state, dependency/conflict checks, unique ownership, invocation routing, and redacted snapshots / 显式多单元安装/启用/停用/卸载状态、依赖/冲突检查、唯一主责、调用路由与脱敏 snapshot | Process-local only; no package discovery/installation, hooks, automatic dependency activation, persistence, remote code, UI, or backend / 仅进程内；没有包发现/安装、hook、自动依赖启用、持久化、远程代码、UI 或后端 |
| `packages/adoption-runtime` | Complete adoption-profile validation, exact explicit-unit matching, dependency-first candidate activation, bounded presentation, receipt, and atomic in-memory replacement / 完整 adoption-profile 校验、精确显式单元匹配、依赖优先候选启用、受限呈现、receipt 与原子进程内替换 | No discovery, package-manager action, dynamic import, hook, persistence, migration, component loading, or external state / 没有发现、包管理器动作、动态 import、hook、持久化、迁移、组件加载或外部状态 |
| `packages/app-integration` | Complete application-template validation, exact slot/state/implementation/surface matching, adoption delegation, and shell bridge fixed to the primary module / 完整应用模板校验、精确 slot/state/implementation/surface 对应、采用委托与固定主模块 shell bridge | No file loading, discovery, generator, package installation, source fallback, component loading, transport, storage, or external write / 没有文件加载、发现、生成器、包安装、source fallback、组件加载、传输、storage 或外部写入 |
| `modules` | Capability-oriented business ownership and schemas / 面向能力的业务主责与 schema | Separates business semantics from package delivery / 与包交付事实分离 |
| `extensions` | Explicit adapters and other documented extension packages; currently includes one neutral injected-wire fixture / 显式 adapter 与其他有文档的扩展包；当前包含一个中性注入式 wire fixture | No hidden coupling to application internals, real backend discovery, or implicit fallback / 不得隐式耦合应用内部实现、发现真实后端或隐式 fallback |
| `templates/example-catalog-query-detail-mp-weixin` | Versioned reusable template plus complete explicit mock/wire candidate assembly / 版本化可复用模板与完整显式 mock/wire 候选装配 | Private workspace package only; no app generation, scaffold copying, published app, industry preset, remote catalog, or fallback / 仅私有 workspace package；没有应用生成、脚手架复制、已发布应用、行业预置、远端目录或回退 |
| `apps/example-catalog-query-detail-mp-weixin` | Versioned declarative app profile, template consumption, registered presentation projection, and controlled UniApp Vue 3 compiler fixture / 版本化声明式 app profile、模板消费、已登记呈现投影与受控 UniApp Vue 3 compiler fixture | Representative `mp-weixin` only; no duplicated provider/lifecycle assembly, dev server, source fallback, router, real backend adapter, real identity, write flow, arbitrary component/script, or release claim / 仅代表性 `mp-weixin`；没有重复 provider/lifecycle 装配、dev server、source 回退、router、真实后端 adapter、真实身份、写流程、任意组件/脚本或发布声明 |
| HIA-uView | Presentational UI input to the controlled fixture / 受控 fixture 的呈现 UI 输入 | Uses a reviewed local source guard, one-use local link, explicit SFC imports, and explicit style; no external `file:` package dependency / 使用已复审的本地 source guard、一次性本地 link、显式 SFC 导入和显式样式；没有外部 `file:` package dependency |

The detailed decision is [ADR-0001: Business composition and contract boundaries](adr/ADR-0001-biz-composition-and-contract-boundaries.md). It defines the module/implementation manifest split, canonical adapter outcomes, profile and channel projection, and declarative dynamic-presentation boundary.

详细决定见 [ADR-0001：业务组合与契约边界](adr/ADR-0001-biz-composition-and-contract-boundaries.md)。该 ADR 定义模块/实现包 manifest 分离、adapter 规范化结果、profile 与渠道投影，以及声明式动态呈现边界。

The application-template layer adds a separate integration boundary above adoption. One template fixes the profile identity, primary module, complete capability slots, required implementation surfaces, host presentation allowlists, routes, and empty screen-capability policies. The private template package assembles either the mandatory mock or explicit injected-wire candidate with neutral reference-data. The generic integration runtime verifies template and candidate correspondence before adoption, delegates atomic activation/replacement, and creates the shell last. The representative app now owns only its app-profile validation, fixture selection, query values, registered-block predicate, and bounded observation; it does not duplicate manifests, providers, lifecycle transitions, routes, or the shell bridge. The template remains data plus reviewed assembly code, not an executable DSL, generator, installer, remote catalog, or source-discovery mechanism.

application-template 层在 adoption 之上增加独立集成边界。一个模板固定 profile identity、主模块、完整 capability slots、必需 implementation surfaces、宿主呈现 allowlist、路由与空 screen-capability policy。私有模板包使用中性 reference-data 装配必备 mock 或显式 injected-wire 候选。通用 integration runtime 在采用前校验模板与候选对应，委托原子启用/替换，并最后创建 shell。代表性 app 现在只拥有 app-profile 校验、fixture 选择、query 值、已登记 block 判断与受限 observation；它不再复制 manifest、provider、lifecycle 转换、路由或 shell bridge。模板仍是数据加经复审的装配代码，而不是可执行 DSL、生成器、安装器、远端目录或 source-discovery 机制。

See the [representative slice contract / 代表性纵切契约](contracts/representative-mp-weixin-slice.md), [application-template contract / 应用模板契约](contracts/application-template.md), [application-integration API / 应用集成 API](api/app-integration.md), and the ADR index for the implemented surface and its deliberate limits.

实现的 API 及其刻意限制见[代表性纵切契约](contracts/representative-mp-weixin-slice.md)、[应用模板契约](contracts/application-template.md)、[应用集成 API](api/app-integration.md)与 [ADR 索引](adr/README.md)。
