# HIA-uView-Biz Core

This package boundary is reserved for the configurable business-framework core. It contains no runtime implementation or stable public API yet.

Before code is added, the composition model, HIA-uView integration contract, module lifecycle, extension boundary and license policy must be recorded and validated.
